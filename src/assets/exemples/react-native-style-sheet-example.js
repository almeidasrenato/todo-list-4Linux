import React from 'react'
import { View, StyleSheet } from 'react-native'

export default function Example() {
    return <View style={styles.example}>...</View>
}

const styles = StyleSheet.create({
    example: {
        backgroundColor: 'red',
    },
})
